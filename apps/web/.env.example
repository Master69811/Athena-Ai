# Athena AI — Frontend environment variables
# Copy to .env.local for local dev, set in Vercel dashboard for production.

# URL of the deployed NestJS API (Render)
NEXT_PUBLIC_API_URL="https://athena-api.onrender.com"
